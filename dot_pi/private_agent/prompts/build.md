---
description: Execute an approved implementation plan
argument-hint: "[plan file]"
---

Invoke `agent-skills:incremental-implementation`.

Use `$ARGUMENTS` if provided; otherwise use `tasks/plan.md`.

Delegate each bounded implementation task to `general` when suitable.

The main session owns task ordering, integration, and final verification.

Implement only approved work.
Stop after completing the current implementation phase.
